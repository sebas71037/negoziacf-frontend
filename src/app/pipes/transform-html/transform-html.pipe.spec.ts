import { TransformHtmlPipe } from './transform-html.pipe';

describe('TransformHtmlPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformHtmlPipe();
    expect(pipe).toBeTruthy();
  });
});
